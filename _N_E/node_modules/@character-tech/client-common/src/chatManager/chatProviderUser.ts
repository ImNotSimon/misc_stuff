import { Participant } from '../types/app-api';

export let currentUser: Participant;

// this is kinda a gnarly way of getting this property
// TODO: implement better mechanism for this
export const SetChatProviderUser = (user: Participant) => {
  currentUser = user;
};
